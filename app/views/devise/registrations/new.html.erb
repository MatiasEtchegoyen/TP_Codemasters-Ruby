<div class="w-full min-h-[100vh] pt-24 registro-page">
  <div class="max-w-xl w-full mx-auto shadow-lg bg-gray-200 rounded-lg py-8 registro-container">
    <h2 class="text-xl mb-4 text-center registro-header">Sign up</h2>

    <div class="w-3/4 mx-auto registro-form">
      <%= form_for(resource, as: resource_name, url: registration_path(resource_name)) do |f| %>
        <%= render "devise/shared/error_messages", resource: resource %>

        <div class="field mb-4 registro-field">
          <%= f.label :email, class: "mb-2 text-lg registro-label" %><br />
          <%= f.email_field :email, autofocus: true, autocomplete: "email", class: "w-full rounded-lg bg-gray-50 registro-input" %>
        </div>

        <div class="field mb-4 registro-field">
          <%= f.label :password, class: "mb-2 text-lg registro-label" %>
          <% if @minimum_password_length %>
          <em class="registro-hint">(<%= @minimum_password_length %> characters minimum)</em>
          <% end %><br />
          <%= f.password_field :password, autocomplete: "new-password", class: "w-full rounded-lg bg-gray-50 registro-input" %>
        </div>

        <div class="field mb-4 registro-field">
          <%= f.label :password_confirmation, class: "mb-2 text-lg registro-label" %><br />
          <%= f.password_field :password_confirmation, autocomplete: "new-password", class: "w-full rounded-lg bg-gray-50 registro-input" %>
        </div>

        <div class="actions registro-actions">
          <%= f.submit "Sign up", class: "bg-blue-500 w-full p-2 text-blue-100 rounded-lg cursor-pointer hover:bg-blue-700 hover:text-blue-300 registro-button" %>
        </div>
      <% end %>

      <div class="flex flex-col items-center mt-4 gap-4 registro-links">
        <%= render "devise/shared/links" %>
      </div>
    </div>
  </div>
</div>
